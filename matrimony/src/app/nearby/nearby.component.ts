import { Component } from '@angular/core';
import { UserserviceService } from '../userservice.service';

@Component({
  selector: 'app-nearby',
  templateUrl: './nearby.component.html',
  styleUrls: ['./nearby.component.css']
})
export class NearbyComponent{
//  implements OnInit {
//   nearbyUsers: any[] = []; // Assuming nearby users data structure
  
//   constructor(private userservice: UserserviceService) { } // Inject the user service

//   ngOnInit(): void {
//     // Fetch nearby users from the service or API
//     this.userservice.getNearbyUsers().subscribe(
//       (users: any[]) => {
//         this.nearbyUsers = users;
//       },
//       (error: any) => {
//         console.error('Error fetching nearby users:', error);
//       }
//     );
 }